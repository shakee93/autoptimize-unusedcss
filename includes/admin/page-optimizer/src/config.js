export default {
    public_base : window.rapidload_admin?.page_optimizer_base ? window.rapidload_admin?.page_optimizer_base + '/../' : '/',
    is_plugin : window.rapidload_admin?.page_optimizer_base ? true : false
}
