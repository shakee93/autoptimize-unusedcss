export default {
    public_base : window.rapidload_admin?.frontend_base ? window.rapidload_admin?.frontend_base + '/' : '/../',
    is_plugin : window.rapidload_admin?.frontend_base ? true : false
}
